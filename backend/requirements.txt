# ============================================
# AI 旅行攻略生成系统 - 后端依赖
# ============================================

# ---------- 基础框架 ----------
fastapi>=0.110.0          # Web 框架（含 StreamingResponse 实现 SSE）
uvicorn[standard]>=0.29.0 # ASGI 服务器
pydantic>=2.6.0           # 数据验证 (v2)
pydantic-settings>=2.2.0  # 配置管理
python-dotenv>=1.0.1      # .env 文件加载（密钥管理，不提交到 Git）

# ---------- LLM / Agent ----------
# Agent 编排使用 LangGraph（StateGraph + 条件边，见 src/agents/graph.py）；
# LLM 调用不依赖 LangChain，节点内直接通过 OpenAI SDK 协议调用（千问兼容模式）。
langgraph>=0.6.0           # Agent 状态图编排（StateGraph / 条件边 / astream）
openai>=1.30.0            # OpenAI SDK（同时用于千问兼容模式）
dashscope>=1.20.0         # 阿里 DashScope (千问备选)

# ---------- Embedding / 向量数据库 (RAG) ----------
sentence-transformers>=3.0.0 # 本地 Embedding 模型 (bge-small-zh-v1.5)
qdrant-client>=1.9.0      # Qdrant 向量数据库客户端（生产环境）
chromadb>=0.5.0           # ChromaDB (开发环境轻量部署)

# ---------- 数据库 ----------
sqlalchemy>=2.0.0         # ORM
aiosqlite>=0.20.0         # 异步 SQLite 驱动

# ---------- 工具 & HTTP ----------
requests>=2.32.0          # HTTP 请求 (高德 API 等)
httpx>=0.27.0             # 异步 HTTP
beautifulsoup4>=4.12.0    # HTML 解析 (百科抓取)
lxml>=5.2.0               # HTML/XML 解析加速

# ---------- 数据处理 ----------
pandas>=2.2.0             # 数据处理
numpy>=1.26.0             # 数值计算

# ---------- 日志 & 工具 ----------
loguru>=0.7.2             # 优雅日志
tenacity>=8.3.0           # 重试装饰器
python-multipart>=0.0.9   # FastAPI 文件上传支持
colorama>=0.4.6           # Windows 控制台颜色
hashids>=1.3.1            # 短链接编码

# ---------- 开发 & 测试 ----------
pytest>=8.2.0             # 测试框架
pytest-asyncio>=0.23.0    # 异步测试
pytest-cov>=5.0.0         # 覆盖率
black>=24.4.0             # 代码格式化
flake8>=7.0.0             # 代码检查
mypy>=1.10.0              # 类型检查
